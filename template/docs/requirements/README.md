# 需求决策记录

每个完整 GitHub Issue 对应一个 `REQ-<三位 Issue 编号>-<slug>/` 目录：

- `design.md`：普通需求统一的产品、技术、数据或素材、风险、测试和验收方案；显式 Pattern 需求不重复
  建立逐 Issue Spec。
- `delivery.md`：结果、风险、Gate 与独立验证证据；实现完成前不存在或保持 `pending`。

GitHub Issue、确定性分支、PR、评论、时间线与 Checks 承载实时状态。
