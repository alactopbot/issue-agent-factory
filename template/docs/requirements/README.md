# 需求方案记录

每个完整 GitHub Issue 对应一个 `REQ-<三位 Issue 编号>-<slug>/` 目录：

- `design.md`：普通需求统一的产品、技术、数据或素材、风险、测试和验收方案；显式 Pattern 需求不重复
  建立逐 Issue Spec。

交付结果、风险、Gate 与独立验证证据保存在 PR body、验证评论和标签中。GitHub Issue、确定性分支、PR、
评论和时间线承载 Factory 实时状态；项目已有 Checks 继续承载项目自己的 CI 状态。
