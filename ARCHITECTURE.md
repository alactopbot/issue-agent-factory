# 架构

```text
GitHub Issue
    │ triage
    ▼
原子认领确定性分支
    ├── 普通需求 ──► 统一 Spec ──► 唯一 Draft PR ──► 人工评论 / Ready ──┐
    │                                                                  │
    └── 显式 Pattern 标签 ──► 已启用 Pattern 授权 ─────────────────────┤
                                                                       ▼
                            完整实现 ──► Gate ──► 独立验证 ──► 人工合并
```

三层职责互不替代：

- GitHub 控制面保存实时状态、人工决定、原子认领对象与不可变检查证据。
- 仓库契约保存项目边界、用户显式授权的 Pattern、普通需求 Spec 和确定性脚本。
- Agent skills 解释状态并执行流程，但执行会话本身不保存授权。

实现步骤不是流程对象。只有用户可独立验收的产品结果才拥有 Issue/分支/PR。是否省略方案确认只由
用户已合并、启用并通过 Issue 标签显式选择的 Pattern 决定；独立验证和人工合并始终保留。
